# 实际环境配置（含模型密钥）不入库
.env

# 依赖与锁产物
node_modules/

# 原生运行产物（报告、截图等），不作为经验资产入库
midscene_run/
.midscene/

# 经验运行动态数据不入库（目录说明保留）；示例资产在 tests/fixtures/ 下受控管理
experiences/*
!experiences/README.md

# TypeScript / 测试临时产物
*.tsbuildinfo

# macOS
.DS_Store

# 编写工作台产物（构建生成）与本地数据（草稿、上传、证据、导出）
src/workbench/web/dist/
artifacts/
