## 1. 输入与评估准备

- [ ] 1.1 核对已交付模型/Promotion 资产，在当前平台验证本地图像依赖并锁版本；以样本解码/裁剪/比较成功且不使用网络服务验证。
- [ ] 1.2 建立原图、±1%/±3% 位移、相似图标、目标移除、状态反转、遮挡和结构变化夹具；按原始捕获分离校准/验证组，交付标注框和数据摘要。

## 2. 本地匹配

- [ ] 2.1 实现环境/页面筛选及动态区固定掩码规则；验证不同页同图标、环境不兼容和错误掩盖目标均拒绝。
- [ ] 2.2 实现有限范围目标模板搜索与当前 bbox 输出；验证位移正例中心落入标注目标且不会直接返回历史坐标。
- [ ] 2.3 实现上下文、局部状态、分项硬阈值和候选歧义判定；用重复目标、通用二态控件状态反转及单项高分负例验证拒绝。
- [ ] 2.4 将 OCR 默认关闭并明确文本证据规则；如开启本地 OCR，验证模型/语言版本固定、文本不符拒绝且网络调用为零，若不启用则文档说明支持边界。
- [ ] 2.5 实现结构化 no-match/错误与输入资源上界；验证坏图、非法配置和搜索边界不会产生可执行坐标。

## 3. 冻结验证

- [ ] 3.1 仅用校准集确定并冻结参数，在独立验证集运行逐例检查；所有声明正例正确定位、全部负例零错误接受，以配置/数据版本和结果文件为完成依据。
- [ ] 3.2 记录本地耗时及重复运行确定性，验证无模型请求和设备动作；运行类型检查并交付 matcher 调用契约和已知限制。
