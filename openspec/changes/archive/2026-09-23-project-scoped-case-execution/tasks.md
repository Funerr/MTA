# Tasks

## 1. 用例结构与选择层

- [x] 1.1 实现项目声明 schema（`cases/<项目>/project.yaml`：platform + devices）与校验，缺失/非法在启动前报错；验证：单元测试覆盖合法声明、缺失声明、非法 platform、非法 devices 四类场景
- [x] 1.2 实现英文 kebab-case 命名规范校验（业务根路径任一级非 ASCII 即拒绝），骨架模板生成入口的命名约束一致；验证：单元测试覆盖含中文路径拒绝与合法路径通过
- [x] 1.3 重构 `cases.config.ts` 发现逻辑：按项目声明推导执行项目、目标（项目/大模块/特性/用例）解析为文件清单并按执行项目收窄，退役 level glob 与平台后缀 glob；验证：单元测试覆盖单条/模块/特性/项目粒度解析、跨项目分组、examples/ 根沿用既有推断
- [x] 1.4 实现用例头部设备需求（`devices:`）解析与项目绑定比对，未满足在任何设备操作前失败；验证：单元测试覆盖需求与绑定一致、缺失别名、未绑定设备三场景

## 2. 统一入口 scripts/run-case.mjs

- [x] 2.1 实现命名空间目标解析（可省略 `.yaml`、多目标、业务根与演示根混用分组），无法解析显式报错；验证：单元测试覆盖单条/大模块/多目标/目标不存在四类
- [x] 2.2 实现前置自检（模型配置、设备连接与调试），失败以使用者语言给单一修复动作并阻止执行；验证：无 `.env` 与无设备两种失败模式输出逐条核对（不启动任何执行）
- [x] 2.3 实现交互菜单（内置 readline，编号下钻 + 四大模块中文标签）与非 TTY 降级清单，非交互不阻塞；验证：脚本化伪终端覆盖菜单下钻执行与无 TTY 清单退出两种行为
- [x] 2.4 实现人话步骤进度、`--verbose` 日志分层、结束摘要（逐条结果、失败步骤、预期 vs 实际）与报告自动打开（`--no-open` / 非 TTY 关闭，打开失败打印路径）；验证：examples/ 演示冒烟核对进度、摘要、报告打开与退出码（2026-09-23 真机补验，见验收文档追加记录）
- [x] 2.5 实现 `--new-project` 骨架生成（project.yaml 模板 + `protocols`/`system`/`core`/`stability` 目录），自定义增删模块不受影响；验证：生成产物断言 + 删除模板模块后选择执行不受影响

## 3. 批量别名收编

- [x] 3.1 `package.json` 的 `test:cases:*` 收编为兼容别名转发统一入口，`MTA_SUITE` / `--project` / `--config` 用户面显式报错并给迁移指引；验证：旧命令组合逐一核对报错文案与退出码，附 README 对照表
- [x] 3.2 `MTA_CASE_FILES` 等内部契约收起为实现细节，清理文档与 Skill 中的暴露；验证：文档 grep 无残留 + 内部选择行为单元测试不变

## 4. 文档与 Skill

- [x] 4.1 改写 README「运行业务用例」与 `cases/README.md`：项目结构、`pnpm case` 一张「意图 → 命令」表、旧维度迁移对照；验证：相对链接核对 + level/后缀旧说法 grep 清零
- [x] 4.2 同步 `examples/README.md` 入口段落（演示根兼容说明）；验证：命令与链接核对，演示语义描述与 D7 一致
- [x] 4.3 改写 `run-case` Skill（命名空间目标映射、失败模式表、菜单口径、自检与报告说明）并更新 `case-to-yaml` 互指边界；验证：Skill 失败模式表与实际报错逐条核对（含无设备验证）
- [x] 4.4 同步 AGENTS.md 中 level/后缀相关表述与 cases/ 示例登记规则；验证：差异核对，相对链接与能力声明一致

## 5. 集成验证

- [x] 5.1 运行 `pnpm typecheck`、`pnpm test`（含新增单元/契约测试）与 `openspec validate --strict`，修复失败并保存结果；验证：三者全绿的输出记录
- [x] 5.2 无设备失败模式全集核对（目标不存在、声明缺失、命名不合规、前置缺失、旧维度报错）；验证：逐条命令核对报错文案与非零退出码，均在执行前失败
- [x] 5.3 Android 真机冒烟：单条用例、大模块、菜单下钻三路径各跑通一次；验证：带日期、设备、应用、模型与覆盖范围的记录追加到验收文档，不改写历史记录（2026-09-23 三路径全部真机跑通；期间发现并修复共享 Agent 报告源归属缺陷，见验收文档追加记录）
- [x] 5.4 HarmonyOS 真机冒烟（环境可用时）：同 5.3 口径单独记录；验证：独立记录追加验收文档，环境缺失时保留未完成状态，不以 Android 结果代替（2026-09-23 用户裁决关闭：平台差异在执行层、入口层平台无关，不重复双平台真机冒烟）
