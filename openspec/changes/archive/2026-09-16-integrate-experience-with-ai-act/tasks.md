## 1. 兼容性关口

- [x] 1.1 确认框架 MVP 集成矩阵和故障验收已通过；核对锁定版本官方 aiAct 定义/执行接口，交付项目范围包装可保持 schema、结果、报告和取消的最小契约试验。
- [x] 1.2 整理支持输入/options/context/结果资格矩阵及必须旁路的情况；用图片、动态返回、内嵌断言和未知参数样本验证原生透传，不用裁剪输入换取缓存命中。

## 2. 透明注册与降级

- [x] 2.1 实现项目级默认关闭开关，关闭时保留原始定义且不创建 Runtime/Store；验证配置加载/Node 参考无需设备及经验资产。
- [x] 2.2 启用时包装官方 aiAct 执行入口，绑定独立 native 回调并复用 Runtime；验证 HIT 无 AI、MISS 一次 AI、回退不递归、只发布一次 Promotion。
- [x] 2.3 保留原生 schema、输入归一化和输出格式，接入原生 signal/截止时间与报告步骤；用原生/包装对照测试核对非法输入、返回文本、错误和超时清理。
- [x] 2.4 保留 experienceAct 和原生 aiAssert，核对逻辑 Key 在同一用例位置更换节点名后兼容；验证跨用例身份不误复用以及其他项目/直接 Agent 调用不受影响。

## 3. 回归与交付

- [x] 3.1 对同一 Node 输入和最小 YAML 解析夹具验证关闭/开启模式，完成原生参数与 Runtime 组合矩阵；记录重放模型请求为零和报告关联，确认 aiAssert 未被拦截，不交付业务 YAML。
- [x] 3.2 演练关闭开关回到原生路径，运行类型检查、相关测试和 Node 参考生成；交付启用范围、旁路条件、升级验证和回滚说明。
