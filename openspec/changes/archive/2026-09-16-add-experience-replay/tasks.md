## 1. 前置与原生动作映射

- [x] 1.1 核对模型、Promotion 和 Matcher 交付证据，建立支持动作与原生直接调用入口的对应表；用锁定版本的最小调用验证参数无损且不触发 AI 定位。
- [x] 1.2 实现整链预检，覆盖未知动作、缺图、非法参数和不兼容版本；验证任一后续动作不支持时首动作也不派发。

## 2. 逐步回放与结果

- [x] 2.1 实现每步新截图、页面/目标/状态验证、当前坐标计算和原生动作派发；用有序截图替身验证坐标来自对应当前帧、Back/Home 仍检查页面。
- [x] 2.2 实现动作后稳定等待、后置证据和最后终态检查；验证无变化、超时、目标位移及最后一动作失败不会误报整链成功。
- [x] 2.3 实现完成数、失败阶段及 none/confirmed-partial/unknown 分类；在派发前失败、派发后异常和截图失败注入下验证无多余动作。
- [x] 2.4 接入原生取消信号和剩余时间，检查等待及动作前后取消；验证不重置预算、不继续派发、不自动重试或调用 AI。
- [x] 2.5 提供逐步事件和截图记录回调；通过原生报告接入夹具验证证据可查看，模块本身不渲染报告或改 Store。

## 3. 框架集成与回归

- [x] 3.1 使用完整 candidate 和可控截图序列驱动框架集成测试，加载实际原生动作接口并在设备传输边界记录派发；验证逐步坐标、终态检查、无模型请求及报告关联，明确替身覆盖范围。
- [x] 3.2 运行类型检查及状态机/参数映射测试，交付 ReplayResult 和动作支持矩阵；失败时明确资源/副作用状态而非只报告 false。
