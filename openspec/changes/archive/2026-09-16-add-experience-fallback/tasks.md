## 1. Runtime 与实验接入

- [x] 1.1 核对四个前序模块及原生 Promotion/Replay 契约，提供使用方资格策略接口且默认策略为空；以注入通用测试策略、未登记/含判断请求走原生验证边界。
- [x] 1.2 实现 Lookup、入口候选选择和 Runtime；验证空 Store、candidate/active/stale、坏资产、环境不兼容及歧义分支，不以设备试点选候选。
- [x] 1.3 实现最多一次 native 调用及剩余预算传递；用动作前拒绝、已确认部分失败、unknown effect、取消和 timeout 验证回退/停止契约。
- [x] 1.4 实现激活、stale、幂等统计与 AI 后 Promotion；验证设备错误不污染视觉状态、写盘失败不改 UI 成功、后缀不冒充完整入口链。
- [x] 1.5 注册 experienceAct 并保留原生 aiAct/aiAssert；用 Node 输入、注册和参考生成夹具验证接入，不交付业务用例。

## 2. 观测与原生契约

- [x] 2.1 接入原生报告事件和调用级模型观测；通过实际原生路径的受控传输测试验证计数覆盖与角色归因，未知记录不能记为零。
- [x] 2.2 验证原生失败、Promotion 失败、Store 不可用、断连和清理路径；检查结果、错误、原生回调次数及未追加设备操作。

## 3. 框架组合验收

- [x] 3.1 建立临时 Store、固定截图/轨迹、可控时间与设备/模型边界，组合实际经验模块；检查夹具来源及原生契约一致，不全量 mock 模块。
- [x] 3.2 验证空库学习与有效重放激活，记录修订、统计、报告事件及重放模型请求为零；测试不包含业务判断。
- [x] 3.3 验证小位移、新入口失配回退、更新后命中及中途后缀入口隔离；断言派发坐标、原生调用次数和资产状态符合规格。
- [x] 3.4 运行完整取消/超时/未知副作用/发布失败矩阵与类型检查；交付框架验证报告并标明受控边界，完成条件不依赖设备业务执行。
