## 1. 官方扩展契约核对

- [x] 1.1 使用当前锁定的 Midscene Test 包构造受控边界验证：别名化 `createMidsceneNodes` 产物后仍保留输入校验、执行与轨迹，并确认公开 `NodeDefinition.execute` 可供并行 Node 委托；以原生边界集成测试通过作为完成证据，若契约不成立先修订设计而不调用私有 API。
- [x] 1.2 核对并记录 `defineNode` 的父步骤超时、取消、失败结果和轨迹行为；以覆盖成功、子失败及超时的最小受控用例和测试输出验证并行报告方案。

## 2. 多设备项目与会话

- [x] 2.1 定义可配置的别名、平台、设备 ID 环境引用及校验规则；验证至少两台、别名格式、缺失 ID、重复物理目标和不同平台组合的单元测试通过，模块导入不连接设备。
- [x] 2.2 在项目 setup 中复用 Android/Harmony 的精确选择、Agent 建立和 `SessionHandle` 清理；验证全部连接成功、后续设备失败、部分连接失败与清理失败的受控测试均保留正确设备信息和原始错误。
- [x] 2.3 在 `midscene.config.ts` 注册独立的 `multi-device` 项目及 `cases/multi-device/` 发现范围；验证配置加载和用例收集不会把现有 Android/Harmony 用例重复归入新项目。

## 3. YAML 目标设备步骤

- [x] 3.1 基于各平台官方 `createMidsceneNodes` 生成 `<alias>.<native-node>`，保留原生 schema、字符串简写和执行轨迹；验证两台同平台、跨平台、交错步骤及平台特有 Node 的边界集成测试通过。
- [x] 3.2 为协作项目提供按别名指向设备的生命周期薄适配，并使未知别名或不支持的 Node 在设备操作前失败；验证相关收集/执行测试，现有单设备 Node 行为测试保持通过。
- [x] 3.3 为每台 Agent 加入在途操作防重叠保护；验证超时后仍在运行的操作不会与下一步骤或下一用例对同一设备的操作重叠。

## 4. 并行步骤

- [x] 4.1 通过官方 `defineNode` 实现 `device.parallel` 的受限输入：至少两个、不同设备、每项一个别名化原生 Node，拒绝嵌套及子步骤 `$`；验证合法与非法 YAML 的收集/输入校验测试。
- [x] 4.2 并发委托已注册的原生 Node、向子调用传递取消、等待汇合并聚合子结果；验证两台设备实际并发、任一失败导致父步骤失败、超时取消通知及后续同设备不重叠。
- [x] 4.3 将各子调用的设备别名、操作名、状态、错误和执行 ID 关联到父步骤结果/错误与原生轨迹；验证成功和混合成功/失败的报告边界集成结果可逐设备定位。

## 5. 接入与验收

- [x] 5.1 更新 Node 参考生成和用例编写说明，明确官方多项目并发与本项目自定义并行 Node 的区别、配置方式、示例 YAML、报告边界和回滚方式；验证 `pnpm run nodes` 生成的协作项目参考包含各别名能力。
- [x] 5.2 运行 `pnpm run typecheck`、`pnpm test` 与配置/参考生成检查，核对现有单设备项目回归和新增协作场景验收；在验收记录中区分受控边界测试与尚未执行的真实设备验证。
