## 1. 前置核对与资产协议

- [x] 1.1 核对 bootstrap 框架验收及已归档规格，记录采用的 Midscene/Node 版本和当前目录；以前置证据链接及契约差异清单为空为完成依据。
- [x] 1.2 实现 Experience/Variant/action 的 v1 运行期校验与字段说明；用完整动作链、缺图、越界 bbox、未知类型/版本和空链夹具验证接受/拒绝结果。
- [x] 1.3 实现请求 Key、逻辑节点别名归一化及环境指纹；验证相同请求稳定、context/参数/环境变化隔离、未知字段不通配及两个入口可共存。

## 2. 文件 Store 与状态

- [x] 2.1 实现根目录受限的资产读写、摘要校验及原子索引发布；验证重启读回、索引替换前中断、损坏引用和越界/符号链接拒绝。
- [x] 2.2 实现 candidate 发布、active/stale 更新、修订冲突和事件幂等统计；验证重复 eventId 不重复计数、旧修订不覆盖新状态、stale 不作为候选。
- [x] 2.3 实现空 Store、版本不支持与 I/O 错误的可区分结果；用临时目录和只读/故障注入验证错误不产生半成品命中。

## 3. 交付验证

- [x] 3.1 运行类型检查和上述协议/持久化测试，记录命令结果；确认无设备/模型依赖且工程安装仍通过。
- [x] 3.2 交付字段文档、完整示例资产和读取说明，并与 Promotion 规划核对参数/前后截图/入口契约；以示例可往返和文档字段一致为完成依据。
